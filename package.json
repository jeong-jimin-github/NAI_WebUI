{
  "name": "nai-webui",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Static NovelAI Image API WebUI with V5 support",
  "scripts": {
    "check": "node --check public/static-adapter.js && node --check public/app.js"
  },
  "engines": { "node": ">=24" }
}
