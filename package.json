{
  "name": "nai-webui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "NovelAI Image API WebUI with V5 support",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "hash-wasm": "^4.12.0"
  },
  "engines": { "node": ">=20" }
}
