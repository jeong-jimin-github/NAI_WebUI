const $ = (s, root = document) => root.querySelector(s);
const $$ = (s, root = document) => [...root.querySelectorAll(s)];

const MODELS = [
  ['nai-diffusion-5-curated', 'NAI Diffusion V5 Curated', 'V5'],
  ['nai-diffusion-5-full', 'NAI Diffusion V5 Full', 'V5'],
  ['nai-diffusion-4-5-curated', 'NAI Diffusion V4.5 Curated', 'V4.5'],
  ['nai-diffusion-4-5-full', 'NAI Diffusion V4.5 Full', 'V4.5'],
  ['nai-diffusion-4-curated-preview', 'NAI Diffusion V4 Curated', 'V4'],
  ['nai-diffusion-4-full', 'NAI Diffusion V4 Full', 'V4'],
  ['nai-diffusion-3', 'NAI Diffusion V3', 'V3'],
  ['nai-diffusion-furry-3', 'NAI Diffusion Furry V3', 'V3'],
];
const RESOLUTIONS = [['Portrait',832,1216],['Landscape',1216,832],['Square',1024,1024],['Large P',1024,1536],['Large L',1536,1024],['Large S',1472,1472],['Wallpaper P',1088,1920],['Wallpaper L',1920,1088],['Small',640,640]];
const QUALITY = {
  'nai-diffusion-5-curated':'masterpiece, very aesthetic, no text, rating:general',
  'nai-diffusion-5-full':'masterpiece, very aesthetic, no text',
  'nai-diffusion-4-5-curated':'location, masterpiece, no text, rating:general',
  'nai-diffusion-4-5-full':'very aesthetic, masterpiece, no text',
  'nai-diffusion-4-curated-preview':'rating:general, amazing quality, very aesthetic, absurdres',
  'nai-diffusion-4-full':'no text, best quality, very aesthetic, absurdres',
  'nai-diffusion-3':'best quality, amazing quality, very aesthetic, absurdres',
  'nai-diffusion-furry-3':'{best quality}, {amazing quality}',
};
const UC = [
  'lowres, artistic error, film grain, scan artifacts, worst quality, bad quality, jpeg artifacts, very displeasing, chromatic aberration, multiple views, logo, too many watermarks, negative space, blank page',
  'lowres, worst quality, bad quality, jpeg artifacts, very displeasing, logo, watermark',
  'lowres, bad anatomy, bad hands, extra digits, missing fingers, worst quality, bad quality, jpeg artifacts, multiple views, logo, watermark',
  'lowres, artistic error, worst quality, bad quality, jpeg artifacts, chromatic aberration, multiple views, logo, watermark, bad anatomy, bad hands, @_@, mismatched pupils, glowing eyes',
];
const state = { token:sessionStorage.getItem('naiToken')||localStorage.getItem('naiToken')||'', subscription:null, characters:[], preciseRefs:[], vibeRefs:[], baseImage:null, maskImage:null, imageMode:'img2img', toolImage:null, results:[], selectedResult:null, zoom:1, busy:false };
const refs = { model:$('#modelSelect'), mode:$('#modeSelect'), width:$('#widthInput'), height:$('#heightInput'), steps:$('#stepsInput'), scale:$('#scaleInput'), sampler:$('#samplerSelect'), scheduler:$('#schedulerSelect'), quality:$('#qualityToggle'), alpha:$('#alphaToggle'), variety:$('#varietyToggle'), cfgRescale:$('#cfgRescaleInput'), samples:$('#samplesInput'), seed:$('#seedInput'), ucPreset:$('#ucPreset'), prompt:$('#promptInput'), negative:$('#negativeInput'), generate:$('#generateButton'), results:$('#resultsStack'), viewer:$('#viewer'), empty:$('#emptyState'), history:$('#historyList') };

function init(){
  MODELS.forEach(([value,label,family])=>{const opt=document.createElement('option');opt.value=value;opt.textContent=`${label} · ${family}`;refs.model.append(opt)}); refs.model.value='nai-diffusion-5-curated';
  RESOLUTIONS.forEach(([name,w,h])=>{const b=document.createElement('button');b.className='res-button';b.textContent=name;b.dataset.w=w;b.dataset.h=h;b.title=`${w} × ${h}`;if(w===832&&h===1216)b.classList.add('active');$('#resolutionPresets').append(b)});
  wireEvents(); updateModelSupport(); updateRanges(); updateAuthUi(); renderCharacters(); renderReferences(); if(state.token)refreshSubscription(true);
}
function wireEvents(){
  $$('.tab').forEach(b=>b.addEventListener('click',()=>switchTab(b.dataset.tab)));
  $$('.res-button').forEach(b=>b.addEventListener('click',()=>{refs.width.value=b.dataset.w;refs.height.value=b.dataset.h;$$('.res-button').forEach(x=>x.classList.toggle('active',x===b))}));
  refs.model.addEventListener('change',updateModelSupport); refs.steps.addEventListener('input',updateRanges); refs.scale.addEventListener('input',updateRanges); $('#strengthInput').addEventListener('input',updateRanges); $('#noiseInput').addEventListener('input',updateRanges);
  $('#randomSeed').addEventListener('click',()=>refs.seed.value=randomSeed()); $('#resetSettings').addEventListener('click',resetSettings); $('#addCharacter').addEventListener('click',addCharacter);
  $('#preciseInput').addEventListener('change',e=>addReferenceFiles(e.target.files,'precise')); $('#vibeInput').addEventListener('change',e=>addReferenceFiles(e.target.files,'vibe'));
  $('#baseImageInput').addEventListener('change',async e=>{state.baseImage=await readImageFile(e.target.files[0]);renderSource('#basePreview',state.baseImage)}); $('#maskInput').addEventListener('change',async e=>{state.maskImage=await readImageFile(e.target.files[0]);renderSource('#maskPreview',state.maskImage)}); $('#toolImageInput').addEventListener('change',async e=>{state.toolImage=await readImageFile(e.target.files[0]);renderSource('#toolPreview',state.toolImage)});
  $$('[data-image-mode]').forEach(b=>b.addEventListener('click',()=>{state.imageMode=b.dataset.imageMode;$$('[data-image-mode]').forEach(x=>x.classList.toggle('active',x===b));$('#maskArea').classList.toggle('hidden',state.imageMode!=='inpaint')}));
  $$('.tool-buttons button').forEach(b=>b.addEventListener('click',()=>runDirectorTool(b.dataset.tool))); $('#useSelectedForTool').addEventListener('click',useSelectedForTool); refs.generate.addEventListener('click',generate); refs.prompt.addEventListener('input',onPromptInput); $('#tagLang').addEventListener('change',()=>suggestTags(true));
  $('#authButton').addEventListener('click',()=>state.token?disconnect():showAuth()); $('#anlasButton').addEventListener('click',()=>state.token?refreshSubscription():showAuth()); $('[data-close-modal]').addEventListener('click',hideAuth); $('#authModal').addEventListener('click',e=>{if(e.target.id==='authModal')hideAuth()}); $$('[data-auth-tab]').forEach(b=>b.addEventListener('click',()=>switchAuth(b.dataset.authTab))); $('#tokenForm').addEventListener('submit',connectToken); $('#passwordForm').addEventListener('submit',connectPassword);
  $('#collapseHistory').addEventListener('click',()=>{document.body.classList.toggle('history-collapsed');$('#collapseHistory').textContent=document.body.classList.contains('history-collapsed')?'‹':'›'}); $('#clearHistory').addEventListener('click',clearHistory); $('#zoomIn').addEventListener('click',()=>setZoom(state.zoom+.1)); $('#zoomOut').addEventListener('click',()=>setZoom(state.zoom-.1)); $('#fitView').addEventListener('click',()=>setZoom(1));
  window.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key==='Enter')generate();if(e.key==='Escape')hideAuth()});
}
function switchTab(name){$$('.tab').forEach(b=>b.classList.toggle('active',b.dataset.tab===name));$$('.tab-page').forEach(p=>p.classList.toggle('active',p.dataset.page===name))}
function updateRanges(){$('#stepsOut').value=refs.steps.value;$('#scaleOut').value=Number(refs.scale.value).toFixed(1);$('#strengthOut').value=Number($('#strengthInput').value).toFixed(2);$('#noiseOut').value=Number($('#noiseInput').value).toFixed(2)}
function resetSettings(){refs.width.value=832;refs.height.value=1216;refs.steps.value=28;refs.scale.value=6;refs.sampler.value='k_euler';refs.scheduler.value='karras';refs.quality.checked=true;refs.alpha.checked=false;refs.variety.checked=true;refs.cfgRescale.value=0;refs.samples.value=1;refs.seed.value=-1;refs.ucPreset.value=0;updateRanges();toast('설정을 초기화했습니다.')}
function modelId(){return refs.model.value} function isV5(model=modelId()){return model.startsWith('nai-diffusion-5')} function isV45(model=modelId()){return model.startsWith('nai-diffusion-4-5')} function isV4Family(model=modelId()){return model.startsWith('nai-diffusion-4')||isV5(model)} function canVibe(model=modelId()){return model.startsWith('nai-diffusion-4')&&!isV5(model)} function canPrecise(model=modelId()){return isV45(model)}
function updateModelSupport(){const model=modelId();$('#preciseSupport').classList.toggle('disabled',!canPrecise(model));$('#vibeSupport').classList.toggle('disabled',!canVibe(model));$('#preciseDrop').style.opacity=canPrecise(model)?'1':'.45';$('#vibeDrop').style.opacity=canVibe(model)?'1':'.45';$('#referenceNotice').classList.toggle('hidden',!isV5(model));refs.alpha.disabled=!isV5(model);if(!isV5(model))refs.alpha.checked=false;if(model.includes('furry'))refs.mode.value='fur'}
function randomSeed(){return Math.floor(Math.random()*4294967288)} function normalizeSeed(){const n=Number(refs.seed.value);return Number.isFinite(n)&&n>=0?Math.floor(n):randomSeed()} function snap64(n){return Math.max(64,Math.min(4096,Math.round(Number(n||64)/64)*64))} function appendTags(base,extra){return[base.trim(),extra?.trim()].filter(Boolean).join(', ')}

function addCharacter(){const max=isV5()?22:6;if(state.characters.length>=max)return toast(`현재 모델은 캐릭터 프롬프트를 최대 ${max}개로 제한했습니다.`,'error');state.characters.push({name:`Character ${state.characters.length+1}`,prompt:'',negative:'',x:.5,y:.5});renderCharacters()}
function renderCharacters(){const root=$('#characterList');root.innerHTML='';state.characters.forEach((c,i)=>{const row=document.createElement('div');row.className='character-item';row.innerHTML=`<span class="num">${i+1}</span><label><span>Name</span><input data-k="name" value="${escapeAttr(c.name)}"></label><label><span>Prompt</span><textarea data-k="prompt">${escapeHtml(c.prompt)}</textarea></label><label><span>Negative</span><textarea data-k="negative">${escapeHtml(c.negative)}</textarea></label><label><span>X (0–1)</span><input data-k="x" type="number" min="0" max="1" step="0.01" value="${c.x}"></label><label><span>Y (0–1)</span><input data-k="y" type="number" min="0" max="1" step="0.01" value="${c.y}"></label><button class="remove-char" title="Remove">×</button>`;$$('[data-k]',row).forEach(input=>input.addEventListener('input',()=>{const k=input.dataset.k;c[k]=['x','y'].includes(k)?Math.max(0,Math.min(1,Number(input.value))):input.value}));$('.remove-char',row).addEventListener('click',()=>{state.characters.splice(i,1);renderCharacters()});root.append(row)});$('#charCount').textContent=state.characters.length;if(!state.characters.length)root.innerHTML='<div class="history-empty">+ Character로 캐릭터별 프롬프트를 추가하세요.</div>'}
async function addReferenceFiles(fileList,type){if(type==='precise'&&!canPrecise())return toast('Precise Reference는 현재 V4.5 모델에서 사용하세요.','error');if(type==='vibe'&&!canVibe())return toast('V5에서는 Vibe Transfer가 아직 지원되지 않습니다. V4/4.5를 선택하세요.','error');for(const file of[...fileList].slice(0,8)){const image=await readImageFile(file);if(!image)continue;if(type==='precise')state.preciseRefs.push({...image,type:'character',strength:1,fidelity:1});else state.vibeRefs.push({...image,strength:.6,information:1})}renderReferences()}
function renderReferences(){renderRefList('precise');renderRefList('vibe');$('#refCount').textContent=state.preciseRefs.length+state.vibeRefs.length}
function renderRefList(type){const arr=type==='precise'?state.preciseRefs:state.vibeRefs;const root=$(`#${type}List`);root.innerHTML='';arr.forEach((ref,i)=>{const el=document.createElement('div');el.className='ref-item';const controls=type==='precise'?`<label>Type<select data-k="type"><option value="character" ${ref.type==='character'?'selected':''}>Character</option><option value="style" ${ref.type==='style'?'selected':''}>Style</option><option value="character&style" ${ref.type==='character&style'?'selected':''}>Character & Style</option></select></label><label>Strength<input data-k="strength" type="number" min="0" max="1" step="0.05" value="${ref.strength}"></label><label>Fidelity<input data-k="fidelity" type="number" min="0" max="1" step="0.05" value="${ref.fidelity}"></label>`:`<label>Strength<input data-k="strength" type="number" min="0" max="1" step="0.05" value="${ref.strength}"></label><label>Information<input data-k="information" type="number" min="0" max="1" step="0.05" value="${ref.information}"></label>`;el.innerHTML=`<img src="${ref.dataUrl}"><div class="ref-controls">${controls}</div><button class="ref-remove">×</button>`;$$('[data-k]',el).forEach(input=>input.addEventListener('change',()=>{ref[input.dataset.k]=input.type==='number'?Number(input.value):input.value}));$('.ref-remove',el).addEventListener('click',()=>{arr.splice(i,1);renderReferences()});root.append(el)})}

async function buildPayload(){
  let prompt=refs.prompt.value.trim();const model=modelId(),mode=refs.mode.value;if(mode==='fur'&&!prompt.toLowerCase().includes('fur dataset'))prompt=appendTags('fur dataset',prompt);if(mode==='background'&&!prompt.toLowerCase().includes('background dataset'))prompt=appendTags('background dataset',prompt);if(refs.quality.checked)prompt=appendTags(prompt,QUALITY[model]||'masterpiece, very aesthetic');if(refs.alpha.checked&&isV5(model))prompt=appendTags(prompt,'transparent background, has alpha, alpha transparency');
  let negative=appendTags(UC[Number(refs.ucPreset.value)]||UC[0],refs.negative.value.trim());const seed=normalizeSeed(),width=snap64(refs.width.value),height=snap64(refs.height.value),chars=state.characters.filter(c=>c.prompt.trim());
  const parameters={width,height,n_samples:Math.max(1,Math.min(4,Number(refs.samples.value||1))),seed,extra_noise_seed:seed,sampler:refs.sampler.value,steps:Number(refs.steps.value),scale:Number(refs.scale.value),cfg_rescale:Number(refs.cfgRescale.value||0),noise_schedule:refs.scheduler.value,legacy:false,legacy_v3_extend:false,params_version:isV5(model)?4:3};
  if(refs.sampler.value==='k_euler_ancestral'){parameters.deliberate_euler_ancestral_bug=false;parameters.prefer_brownian=true}if(refs.variety.checked){if(model.includes('4-5'))parameters.skip_cfg_above_sigma=58;else if(model.startsWith('nai-diffusion-4'))parameters.skip_cfg_above_sigma=19}
  if(isV4Family(model)){const charCaptions=chars.map(c=>({char_caption:c.prompt,centers:[{x:c.x,y:c.y}]})),negCharCaptions=chars.map(c=>({char_caption:c.negative||'',centers:[{x:c.x,y:c.y}]}));parameters.add_original_image=true;parameters.legacy_uc=false;parameters.v4_prompt={caption:{base_caption:prompt,char_captions:charCaptions},use_coords:chars.length>0,use_order:true,legacy_uc:false};parameters.v4_negative_prompt={caption:{base_caption:negative,char_captions:negCharCaptions},use_coords:false,use_order:false,legacy_uc:false}}else parameters.negative_prompt=negative;
  if(canPrecise(model)&&state.preciseRefs.length){parameters.director_reference_images=state.preciseRefs.map(r=>r.base64);parameters.director_reference_descriptions=state.preciseRefs.map(r=>({caption:{base_caption:r.type,char_captions:[]},legacy_uc:false}));parameters.director_reference_information_extracted=state.preciseRefs.map(()=>1);parameters.director_reference_strength_values=state.preciseRefs.map(r=>Number(r.strength));parameters.director_reference_secondary_strength_values=state.preciseRefs.map(r=>1-Number(r.fidelity))}
  else if(canVibe(model)&&state.vibeRefs.length){const tokens=[];for(let i=0;i<state.vibeRefs.length;i++){setBusy(true,`Encoding vibe ${i+1}/${state.vibeRefs.length}`);const r=state.vibeRefs[i],response=await api('/api/nai/encode-vibe',{image:r.base64,information_extracted:Number(r.information),model},true);tokens.push(arrayBufferToBase64(await response.arrayBuffer()))}parameters.reference_image_multiple=tokens;parameters.reference_strength_multiple=state.vibeRefs.map(r=>Number(r.strength));parameters.normalize_reference_strength_multiple=true}
  let action='generate',payloadModel=model;if(state.baseImage&&currentImageTabActive()){if(state.imageMode==='img2img'){action='img2img';parameters.image=state.baseImage.base64;parameters.strength=Number($('#strengthInput').value);parameters.noise=Number($('#noiseInput').value)}else{if(!state.maskImage)throw new Error('Inpaint mask를 먼저 업로드하세요.');action='infill';parameters.image=state.baseImage.base64;parameters.mask=state.maskImage.base64;parameters.add_original_image=true;parameters.inpaintImg2ImgStrength=Number($('#strengthInput').value);parameters.noise=Number($('#noiseInput').value);payloadModel=inpaintModel(model)}}return{input:prompt,model:payloadModel,action,parameters}
}
function currentImageTabActive(){return $('.tab.active')?.dataset.tab==='image'}
function inpaintModel(model){if(model==='nai-diffusion-5-curated')return'nai-diffusion-4-5-curated-inpainting';if(model==='nai-diffusion-5-full')return'nai-diffusion-5-full-inpainting';if(model==='nai-diffusion-4-curated-preview')return'nai-diffusion-4-curated-inpainting';if(model.endsWith('-inpainting'))return model;return`${model}-inpainting`}

async function generate(){if(state.busy)return;if(!state.token)return showAuth();if(!refs.prompt.value.trim()&&!state.baseImage)return toast('프롬프트를 입력하세요.','error');try{setBusy(true,'Preparing');const payload=await buildPayload();setBusy(true,'Generating');const response=await api('/api/nai/generate',payload,true),images=await extractImages(response,`NAI_${Date.now()}`);if(!images.length)throw new Error('API 응답에서 이미지를 찾지 못했습니다.');addResults(images,{model:payload.model,prompt:payload.input,action:payload.action,seed:payload.parameters.seed});refs.seed.value=-1;refreshSubscription(true)}catch(error){toast(cleanError(error),'error',6000)}finally{setBusy(false)}}
async function runDirectorTool(tool){if(state.busy)return;if(!state.token)return showAuth();if(!state.toolImage)return toast('Director Tool 원본 이미지를 선택하세요.','error');try{setBusy(true,`Running ${tool}`);const src=state.toolImage;let response;if(tool.startsWith('upscale')){const scale=tool==='upscale4'?4:2;response=await api('/api/nai/upscale',{image:src.base64,width:src.width,height:src.height,scale},true)}else{const body={req_type:tool,width:src.width,height:src.height,image:src.base64};if(tool==='colorize'){body.prompt=$('#toolPrompt').value;body.defry=Number($('#toolDefry').value||0)}if(tool==='emotion'){body.prompt=`${$('#emotionSelect').value};;${$('#toolPrompt').value}`;body.defry=Number($('#toolDefry').value||0)}response=await api('/api/nai/augment',body,true)}const images=await extractImages(response,`NAI_${tool}_${Date.now()}`);addResults(images,{model:'Director Tool',prompt:tool,action:tool,seed:null});switchTab('prompt');refreshSubscription(true)}catch(error){toast(cleanError(error),'error',6000)}finally{setBusy(false)}}
async function useSelectedForTool(){if(!state.selectedResult)return toast('먼저 생성 결과 하나를 클릭해 선택하세요.','error');state.toolImage=await readBlobAsImage(state.selectedResult.blob,state.selectedResult.filename);renderSource('#toolPreview',state.toolImage);toast('선택 결과를 Tool Source로 지정했습니다.','ok')}

function addResults(images,meta){refs.empty.classList.add('hidden');refs.viewer.classList.remove('empty');const group=document.createElement('div');group.className='result-group';images.forEach(image=>{const result={...image,...meta,id:crypto.randomUUID(),pinned:false,createdAt:Date.now()};state.results.unshift(result);state.selectedResult=result;const card=document.createElement('div');card.className='result-card selected';card.dataset.id=result.id;card.innerHTML=`<img src="${result.url}" alt="Generated image"><div class="result-actions"><button data-a="pin" title="Pin">☆</button><button data-a="tool" title="Use in tools">↗</button><button data-a="download" title="Download">↓</button></div>`;$('img',card).addEventListener('click',()=>selectResult(result.id));$('[data-a=pin]',card).addEventListener('click',e=>{e.stopPropagation();togglePin(result.id)});$('[data-a=tool]',card).addEventListener('click',async e=>{e.stopPropagation();selectResult(result.id);await useSelectedForTool();switchTab('tools')});$('[data-a=download]',card).addEventListener('click',e=>{e.stopPropagation();downloadResult(result)});$$('.result-card').forEach(x=>x.classList.remove('selected'));group.append(card)});refs.results.prepend(group);renderHistory();setZoom(state.zoom);toast(`${images.length}개 이미지 생성 완료`,'ok')}
function selectResult(id){state.selectedResult=state.results.find(r=>r.id===id)||null;$$('.result-card').forEach(c=>c.classList.toggle('selected',c.dataset.id===id))}
function togglePin(id){const r=state.results.find(x=>x.id===id);if(!r)return;r.pinned=!r.pinned;renderPins();renderHistory()}
function renderPins(){const root=$('#pinStrip');root.innerHTML='';const pins=state.results.filter(r=>r.pinned);root.classList.toggle('hidden',!pins.length);pins.slice(0,12).forEach(r=>{const img=new Image();img.src=r.url;img.title=r.prompt;img.addEventListener('click',()=>selectResult(r.id));root.append(img)})}
function renderHistory(){refs.history.innerHTML='';if(!state.results.length)return refs.history.innerHTML='<div class="history-empty">생성한 이미지가 여기에 표시됩니다.</div>';state.results.slice(0,100).forEach(r=>{const el=document.createElement('div');el.className=`history-thumb${r.pinned?' pinned':''}`;el.innerHTML=`<img src="${r.url}" title="${escapeAttr(r.prompt)}">`;el.addEventListener('click',()=>{switchTab('prompt');selectResult(r.id)});refs.history.append(el)})}
function clearHistory(){state.results.forEach(r=>URL.revokeObjectURL(r.url));state.results=[];state.selectedResult=null;refs.results.innerHTML='';refs.empty.classList.remove('hidden');refs.viewer.classList.add('empty');renderHistory();renderPins()} function downloadResult(r){const a=document.createElement('a');a.href=r.url;a.download=r.filename||'novelai.png';a.click()}
async function extractImages(response,prefix){const blob=await response.blob(),type=response.headers.get('content-type')||blob.type||'',bytes=new Uint8Array(await blob.arrayBuffer()),isZip=bytes[0]===0x50&&bytes[1]===0x4b;if(isZip&&window.JSZip){const zip=await window.JSZip.loadAsync(bytes),out=[];let index=0;for(const entry of Object.values(zip.files)){if(entry.dir)continue;const fileBlob=await entry.async('blob'),mimeBlob=fileBlob.type?fileBlob:new Blob([fileBlob],{type:'image/png'});out.push({blob:mimeBlob,url:URL.createObjectURL(mimeBlob),filename:`${prefix}_${index++}.png`})}return out}if(type.includes('image')||bytes.length>8){const imageBlob=new Blob([bytes],{type:type.includes('image')?type:'image/png'});return[{blob:imageBlob,url:URL.createObjectURL(imageBlob),filename:`${prefix}.png`}]}return[]}

async function api(path,body={},binary=false,method='POST'){if(!state.token)throw new Error('NovelAI에 로그인되어 있지 않습니다.');const init={method,headers:{'X-NAI-Token':state.token}};if(method!=='GET'){init.headers['Content-Type']='application/json';init.body=JSON.stringify(body)}const response=await fetch(path,init);if(!response.ok){let message=`${response.status} ${response.statusText}`;try{const data=await response.json();message=data.error||data.message||message}catch{}if(response.status===401||response.status===403)message+=' — 토큰/구독 상태를 확인하세요.';throw new Error(message)}return binary?response:response.json()}
async function refreshSubscription(silent=false){if(!state.token)return;try{const data=await api('/api/nai/subscription');state.subscription=data.subscription;updateAuthUi();if(!silent)toast('Anlas 잔량을 갱신했습니다.','ok')}catch(e){if(!silent)toast(cleanError(e),'error')}}
function updateAuthUi(){$('#authButton').textContent=state.token?'Disconnect':'로그인';if(state.subscription){$('#anlasValue').textContent=Number(state.subscription.anlas).toLocaleString();$('#tierValue').textContent=state.subscription.tierName}else{$('#anlasValue').textContent=state.token?'…':'—';$('#tierValue').textContent=state.token?'확인 중':'로그인 필요'}}
function showAuth(){$('#authModal').classList.remove('hidden');$('#authError').classList.add('hidden')} function hideAuth(){$('#authModal').classList.add('hidden')} function switchAuth(tab){$$('[data-auth-tab]').forEach(b=>b.classList.toggle('active',b.dataset.authTab===tab));$('#tokenForm').classList.toggle('hidden',tab!=='token');$('#passwordForm').classList.toggle('hidden',tab!=='password')}
async function connectToken(e){e.preventDefault();const token=$('#tokenInput').value.trim();if(!token)return authError('토큰을 입력하세요.');try{setAuthBusy(true);state.token=token;const data=await api('/api/auth/token');state.subscription=data.subscription;storeToken(token,$('#rememberToken').checked);updateAuthUi();hideAuth();toast('NovelAI 연결 완료','ok')}catch(err){state.token='';authError(cleanError(err))}finally{setAuthBusy(false)}}
async function connectPassword(e){e.preventDefault();const email=$('#emailInput').value.trim(),password=$('#passwordInput').value;if(!email||!password)return authError('ID/이메일과 비밀번호를 입력하세요.');try{setAuthBusy(true);const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})}),data=await response.json();if(!response.ok)throw new Error(data.error||'Login failed');state.token=data.accessToken;state.subscription=data.subscription;storeToken(state.token,false);$('#passwordInput').value='';updateAuthUi();hideAuth();toast('NovelAI 로그인 완료','ok')}catch(err){authError(cleanError(err))}finally{setAuthBusy(false);$('#passwordInput').value=''}}
function storeToken(token,remember){sessionStorage.setItem('naiToken',token);if(remember)localStorage.setItem('naiToken',token);else localStorage.removeItem('naiToken')} function disconnect(){state.token='';state.subscription=null;sessionStorage.removeItem('naiToken');localStorage.removeItem('naiToken');updateAuthUi();toast('연결을 해제했습니다.')} function authError(message){$('#authError').textContent=message;$('#authError').classList.remove('hidden')} function setAuthBusy(busy){$$('.auth-form button[type=submit]').forEach(b=>b.disabled=busy)}

let suggestTimer;function onPromptInput(){$('#promptLength').textContent=refs.prompt.value.length;clearTimeout(suggestTimer);suggestTimer=setTimeout(()=>suggestTags(false),280)}
async function suggestTags(force){if(!state.token)return;const text=refs.prompt.value,q=text.split(',').pop().trim();if(!force&&q.length<2)return hideSuggestions();try{const params=new URLSearchParams({model:modelId(),prompt:q,lang:$('#tagLang').value}),response=await fetch(`/api/nai/tags?${params}`,{headers:{'X-NAI-Token':state.token}});if(!response.ok)return hideSuggestions();const data=await response.json();renderSuggestions(data.tags||[],q)}catch{hideSuggestions()}}
function renderSuggestions(tags,q){const root=$('#tagSuggestions');root.innerHTML='';if(!tags.length)return hideSuggestions();tags.slice(0,12).forEach(t=>{const tag=typeof t==='string'?t:(t.tag||t.value||t.name||'');if(!tag)return;const b=document.createElement('button');b.innerHTML=`<span>${escapeHtml(tag)}</span><small>${typeof t==='object'?(t.count??t.confidence??''):''}</small>`;b.addEventListener('mousedown',e=>{e.preventDefault();const before=refs.prompt.value.slice(0,refs.prompt.value.lastIndexOf(',')+1);refs.prompt.value=`${before}${before?' ':''}${tag}, `;refs.prompt.focus();hideSuggestions();onPromptInput()});root.append(b)});root.classList.remove('hidden')} function hideSuggestions(){$('#tagSuggestions').classList.add('hidden')}
function setBusy(busy,label='Generate'){state.busy=busy;refs.generate.disabled=busy;refs.generate.querySelector('span:nth-child(2)').textContent=busy?label:'Generate'} function setZoom(value){state.zoom=Math.max(.4,Math.min(2,value));refs.results.style.transform=`scale(${state.zoom})`;$('#zoomValue').textContent=`${Math.round(state.zoom*100)}%`}
function toast(message,type='',duration=3600){const el=document.createElement('div');el.className=`toast ${type}`;el.textContent=message;$('#toastLayer').append(el);setTimeout(()=>el.remove(),duration)} function cleanError(e){const m=e?.message||String(e);try{const parsed=JSON.parse(m);return parsed.message||parsed.error||m}catch{return m}} function escapeHtml(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))} function escapeAttr(s=''){return escapeHtml(s).replace(/`/g,'&#96;')} function arrayBufferToBase64(buffer){const bytes=new Uint8Array(buffer);let binary='';for(let i=0;i<bytes.length;i+=0x8000)binary+=String.fromCharCode(...bytes.subarray(i,i+0x8000));return btoa(binary)}
async function readImageFile(file){if(!file)return null;return readBlobAsImage(file,file.name)} async function readBlobAsImage(blob,name='image.png'){const dataUrl=await new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=reject;r.readAsDataURL(blob)}),img=await new Promise((resolve,reject)=>{const i=new Image();i.onload=()=>resolve(i);i.onerror=reject;i.src=dataUrl});return{name,dataUrl,base64:dataUrl.split(',')[1],width:img.naturalWidth,height:img.naturalHeight,blob}} function renderSource(selector,image){const root=$(selector);root.innerHTML=image?`<img src="${image.dataUrl}" alt="source"><div class="setting-label">${image.width} × ${image.height}</div>`:''}
init();
